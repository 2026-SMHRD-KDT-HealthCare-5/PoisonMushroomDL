# --- 항상 필요 (가볍고, mock 모드에서도 사용됨) ---
fastapi
uvicorn[standard]
python-multipart
pydantic
python-dotenv
Pillow
numpy

# --- 선택: 실제 추론을 쓰려면 설치 (없으면 mock 예측으로 자동 폴백) ---
# tensorflow와 langchain-google-genai(google-generativeai)는 protobuf 요구 버전이 서로 다르므로
# (tensorflow>=6.31, google-ai-generativelanguage<6.0) protobuf를 6.31~7.x 범위로 강제 고정한다.
# 실제로 두 스택 모두 이 범위에서 정상 동작함을 확인했다.
tensorflow
protobuf>=6.31.1,<8.0.0

# --- 선택: 실제 RAG(Gemini+FAISS)를 쓰려면 설치 (없으면 템플릿 답변으로 자동 폴백) ---
langchain-core
langchain-community
langchain-google-genai
faiss-cpu
google-generativeai
